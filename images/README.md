# Screenshots

Referenced from the top-level `README.md`:

- `hero_nushell.png` - the prompt running a structured `ifconfig` (top of the README)
- `zsh.png`          - the two-line prompt with a colored `ls`
- `tmux.png`         - the tmux status bar
- `theme.png`        - the `theme` picker with its live preview
- `zellij.png`       - zellij themed to match
- `ehelp.png`        - the `ehelp` command index
- `swapshell.png`    - the `swapshell` picker
- `e-session-log.png` - the `e-session-log view` session browser
