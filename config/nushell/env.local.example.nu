# Machine-specific env and secrets go here. Copy to env.local.nu (never committed).

# $env.JAVA_HOME = "/opt/homebrew/opt/openjdk@17"
# $env.DIGITALOCEAN_ACCESS_TOKEN = "..."
# $env.HCLOUD_TOKEN = "..."
